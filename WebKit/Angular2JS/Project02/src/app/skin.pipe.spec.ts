/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SkinPipe } from './skin.pipe';

describe('SkinPipe', () => {
  it('create an instance', () => {
    const pipe = new SkinPipe();
    expect(pipe).toBeTruthy();
  });
});
